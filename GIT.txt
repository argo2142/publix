Install GIT:
https://git-scm.com/install/windows
OR
winget.exe install --id "Git.Git" --exact  --accept-source-agreements --disable-interactivity --silent --accept-package-agreements --force
winget.exe install --id "GitHub.GitHubDesktop" --exact --source winget --accept-source-agreements --disable-interactivity --silent --accept-package-agreements --force
winget install --id "github.cli" --silent #required to access Github through VS Code
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"

# Restart VS Code after installing this
# To authenticate VS Code to Github account
At the VS Code Terminal type "GH auth login"
Select "Github.com" (it is the default)
Select "HTTPS (it is the default)
"Do you want to authenticate"... select "Y"
"Login with a web browser"... hit enter
Copy the ONE TIME code and hit enter to launch browser
Paste ONE TIME code and say Yes

To manually push a change from the VS Code command line

Git Add . (this takes everything ... or you could also be specific... Git Add .\readme.md)
Git Commit -m "NintyNinth Commit"
Git push

Go to the file on Github,ccom
# click the "History" button on the right side - this will display all of the commits and their hash IDs

# bring up version desired
# Format: Git Checkout <commit-hash> [--] <pathspec>…
git checkout 757c47d4 -- Publix/git.txt
# This should bring up the commit requested

#to now make this the latest commit (again)

Git Add . (this takes everything ... or you could also be specific... Git Add .\readme.md)
Git Commit -am "100TH Commit" # NOTICE THE "-am"... THIS IS DIFFERENT FROM BEFORE. THAT USED "-a"
Git push
